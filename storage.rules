rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Offen wie Firestore — App-Check-strikte Variante erst mit Key + gleichzeitigem Firestore-Deploy
      allow read, write: if true;
    }
  }
}
