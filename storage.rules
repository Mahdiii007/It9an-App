rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Gleiche Absicherung wie Firestore — App Check im Client nötig (index.html)
      allow read, write: if request.appCheck.valid;
    }
  }
}
